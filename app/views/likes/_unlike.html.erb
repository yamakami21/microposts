<%= form_for(current_user.likes.find_by(micropost_id: micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= hidden_field_tag :micropost_id, micropost.id %>
  <%= f.submit "Unlike" %>
<% end %>